{% extends 'base.html.twig' %}

{% block title %}Liste des Mères{% endblock %}

{% block body %}
<div class="container-fluid mt-5">
    <h1 class="mb-4">Liste des Mères</h1>
    <div class="row">
        <!-- Filtre -->
        <div class="col-md-3">
            <div class="card w-100 p-1">
                <div class="card-body p-1">
                    <h5 class="card-title">Filtrer</h5>
                    {% include "meres/_filter.html.twig" %}
                </div>
            </div>
        </div>

        <!-- Tableau -->
        <div class="col-md-9">
            <div class="card">
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table table-striped table-hover">
                            <thead class="table-dark">
                                <tr>
                                    <th>Id</th>
                                    <th>Nom</th>
                                    <th>Prénom</th>
                                    <th>Profession</th>
                                    <th>téléphone</th>
                                    <th>téléphone</th>
                                    <th>Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for mere in meres %}
                                <tr>
                                    <td>{{ mere.id }}</td>
                                    <td>{{ mere.nom }}</td>
                                    <td>{{ mere.prenom }}</td>
                                    <td>{{ mere.profession ? mere.profession : 'Non défini' }}</td>
                                    <td>{{ mere.telephone1 }}</td>
                                    <td>{{ mere.telephone2 }}</td>
                                    <td>
                                        <a href="{{ path('app_meres_show', {'id': mere.id}) }}" class="btn btn-info btn-sm">Voir</a>
                                        <a href="{{ path('app_meres_edit', {'id': mere.id}) }}" class="btn btn-warning btn-sm">Modifier</a>
                                    </td>
                                </tr>
                                {% else %}
                                <tr>
                                    <td colspan="5" class="text-center">Aucun enregistrement trouvé.</td>
                                </tr>
                                {% endfor %}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Bouton Ajouter -->
    <div class="text-end mt-4">
        <a href="{{ path('app_meres_new') }}" class="btn btn-success">
            <i class="bi bi-plus-circle"></i> Ajouter une nouvelle Mère
        </a>
    </div>
</div>

{% endblock %}