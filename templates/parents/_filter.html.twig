{{ form_start(form, {'attr': {'class': 'form-inline mb-4'}}) }}
<div class="row">
    <div class="row g-3">
        <!-- Champ de recherche (q) -->
        <!-- Numéro de téléphone -->
        <div class="col-3">
            {{ form_row(form.telephonePere, {'attr': {'class': 'form-control', 'placeholder': 'Numéro de téléphone...'}}) }}
        </div>

        <!-- NINA -->
        <div class="col-3">
            {{ form_row(form.ninaPere, {'attr': {'class': 'form-control', 'placeholder': 'Numéro d\'identification nationale...'}}) }}
        </div>
        <!-- Numéro de téléphone -->
        <div class="col-3">
            {{ form_row(form.telephoneMere, {'attr': {'class': 'form-control', 'placeholder': 'Numéro de téléphone...'}}) }}
        </div>

        <!-- NINA -->
        <div class="col-3">
            {{ form_row(form.ninaMere, {'attr': {'class': 'form-control', 'placeholder': 'Numéro d\'identification nationale...'}}) }}
        </div>
    </div></div>


    <!-- Boutons -->

    <div class="row mt-3">
        <div class="col-12 text-end">
            <div class="row">
                <div class="col-md-6">
                    <button type="submit" class="btn btn-primary w-100">
                        <i class="bi bi-search"></i> Rechercher
                    </button>
                </div>
                <div class="col-md-6">
                    <a href="{{ path('app_parents_index') }}" class="btn btn-secondary w-100">
                        <i class="bi bi-arrow-counterclockwise"></i> Réinitialiser
                    </a>
                </div>
            </div>
        </div>
    </div>
{{ form_end(form) }}