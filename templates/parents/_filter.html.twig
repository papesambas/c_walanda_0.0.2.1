{{ form_start(form, {'attr': {'class': 'form-inline mb-4'}}) }}
<div class="row g-3">
        <!-- Champ de recherche (q) -->
        <div class="col-12">
            {{ form_row(form.qpere, {'attr': {'class': 'form-control', 'placeholder': 'Nom, prénom...'}}) }}
        </div>
    
    <!-- Champ de recherche (q) -->
    <div class="col-12">
        {{ form_row(form.telephonePere, {'attr': {'class': 'form-control', 'placeholder': 'Numéro de téléphone du père...'}})
        }}
    </div>

    <!-- Liste des professions -->
    <div class="col-12">
        {{ form_row(form.ninaPere, {'attr': {'class': 'form-control', 'placeholder': 'Numéro d\'identification nationale...'}})
        }}
    </div>

        <!-- Champ de recherche (q) -->
        <div class="col-12">
            {{ form_row(form.qmere, {'attr': {'class': 'form-control', 'placeholder': 'Nom, prénom...'}}) }}
        </div>    

    <!-- Numéro de téléphone -->
    <div class="col-12">
        {{ form_row(form.telephoneMere, {'attr': {'class': 'form-control', 'placeholder': 'Numéro de téléphone de la mère...'}})
        }}
    </div>

    <!-- NINA -->
    <div class="col-12">
        {{ form_row(form.ninaMere, {'attr': {'class': 'form-control', 'placeholder': 'Numéro d\'identification nationale...'}})
        }}
    </div>
</div>

<!-- Boutons -->

<div class="row mt-3">
    <div class="col-12 text-end">
        <div class=class="d-flex justify-content-end gap-2">
            <button type="button" class="btn btn-secondary" data-action="search-without-submit" data-url="{{ path('app_parents_index_ajax') }}">
                Rechercher sans soumettre
            </button>
            <button type="submit" class="btn btn-sm btn-primary w-100">
                <i class="bi bi-search"></i> Inscription
            </button>
            <a href="{{ path('app_parents_index') }}" class="btn btn-sm btn-secondary w-100">
                <i class="bi bi-arrow-counterclockwise"></i> Réinitialiser
            </a>
        </div>
    </div>
</div>
{{ form_end(form) }}



